# Editor / tool state
.claude/
.playwright-mcp/
.DS_Store

# Playwright artifacts
mobile-game.png
home.png
*.log

# Node
node_modules/

# Expo
mobile/.expo/
mobile/web-build/
mobile/ios/
mobile/android/
# (mobile/dist is the production build — it's regenerated by Vercel from
# source on every deploy, so we don't commit it here either.)
mobile/dist/
